node_modules

# Output
.output
.vercel
.netlify
.wrangler
/build
.svelte-kit
__pycache__
*.egg-info

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
svelte/.vscode
.venv
.uv

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Design files
*.afdesign
*.afdesign~lock~
*.ai

# Throwaway scripts
scratch.*
mprocs.log

out/
.velopack
dist
# web backend venv & local tooling
.venv-backend/
.claude/

# EdgeOne Pages: the function (cloud-functions/api/backend.py + psychopy/) is
# committed directly so EdgeOne's `makers ci` step sees it in the cloned tree
# and wires the /api/backend route. Refresh it with `npm run build:edgeone-output`.

# EdgeOne CLI project/login state
.edgeone/
