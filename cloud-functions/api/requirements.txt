# Python deps EdgeOne installs for the serverless function (cloud-functions/api/backend.py).
#
# PsychoPy itself is vendored under cloud-functions/api/psychopy/ (not pip).
# scipy and pandas are intentionally NOT listed: PsychoPy imports them at module
# load, but the compile/profile command paths never call into them, so the
# function stubs them out (_install_optional_stubs in backend.py). Dropping them
# saves ~167MB and keeps the bundle well under the size limit.
numpy
openpyxl
json-tricks
pyyaml
esprima
dukpy
astunparse
i18next
javascripthon
pyserial
packaging
six
